# ==============================================================================
# 執行時相依 (production runtime dependencies)
#
# 所有版本皆已鎖定，確保環境可重現。
# 開發/測試/壓測用套件請見 requirements-dev.txt。
#
# 升級相依時請一併執行：
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/
# ==============================================================================

# --- LangGraph 核心 ---
langgraph==1.2.10
langgraph-checkpoint-redis==0.5.1  # Redis checkpointer，已從 langgraph 本體拆出

# --- LangChain 核心與整合 ---
langchain-core==1.5.3
langchain-community==0.4.2  # 註：官方已宣布 sunset，OpenSearch/HuggingFace 整合待遷移至獨立套件
langchain-text-splitters==1.1.2
langchain-google-genai==4.3.2
langchain-openai==1.4.1

# --- OpenSearch ---
opensearch-py==3.2.0

# --- FastAPI / Web ---
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2  # src/config.py 的 BaseSettings 來源
httpx==0.28.1

# --- 工具 ---
python-dotenv==1.2.2
async-lru==2.3.0  # rag_chain_service / rag_service 的 alru_cache
tenacity==9.1.4   # 重試機制與指數退避
numpy==2.4.6
tqdm==4.70.0
colorama==0.4.6

# --- 狀態持久化 ---
redis==7.4.1  # 上限由 langgraph-checkpoint-redis → redisvl 決定，不可升到 8.x

# --- 可觀測性：日誌 ---
loguru==0.7.3

# --- 可觀測性：度量 ---
prometheus-client==0.26.0
psutil==7.2.2

# --- 可觀測性：分散式追蹤 ---
# 註：opentelemetry-exporter-jaeger 已被官方 deprecated，且會將 otlp exporter
#     反向鎖回 1.15.0，故移除。Jaeger 本身原生支援 OTLP 接收。
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
